# ShikshaSignal AI — Phase 1 (deterministic core, no AI) dependencies.
# Kept deliberately lean. LLM/RAG/dashboard deps are added in later phases.
pandas>=2.0
numpy>=1.24
pydantic>=2.0
jinja2>=3.1
pyyaml>=6.0
pytest>=7.0
